import ColumnField from './column-field.tsx';
import AutoGeneratedColumns from './auto-generated-columns.tsx';
import ColumnWidth from './column-width.tsx';
import ColumnWidthPixel from './column-width-pixel.tsx';
import ColumnWidthPercentage from './column-width-percentage.tsx';
import DynamicColumnWidth from './dynamic-column-width.tsx';
import ColumnTextAlign from './column-textalign.tsx';
import ColumnVisible from './column-visible.tsx';
import CustomizedAutoGeneratedColumns from './customized-autogenerated-columns.tsx';
import ComplexColumns from './complex-columns.tsx';
import ComplexRemoteColumns from './complex-remote-columns.tsx';
import ComplexNestedColumns from './complex-deeply-nested-columns.tsx';
import { gridComplexBindingSnippet, gridRemoteDataMangerSnippet, gridComplexNextedColumnSnippet } from './grid-snippets.tsx';
import { Preview } from '../../common/preview';
import { Snippet } from '../../common/snippet';
import { Meta } from '../../common/meta';

<Meta
  title="React Data Grid - Columns | Syncfusion"
  description="Data Grid columns define field mapping, alignment, and display behavior to control layout, formatting, and content presentation in grid interfaces."
/>

# Columns in React Data Grid

The Syncfusion® React Data Grid component organizes data in a table using columns. Each column requires a valid field name to support key grid features such as sorting, filtering, and editing. Organizing data into columns allows efficient interaction and manipulation within the grid.

## Defining columns

Columns can be manually configured by setting properties such as width, header text, alignment, formatting, and more, or they can be generated automatically from the data source without requiring manual setup.

### Manually define columns

Columns are configured using the [`Columns`](https://react-api.syncfusion.com/data-grid/overview#columns) component, where each column defines attributes such as [`field`](https://react-api.syncfusion.com/data-grid/columnprops#field), [`headerText`](https://react-api.syncfusion.com/data-grid/columnprops#headertext), [`type`](https://react-api.syncfusion.com/data-grid/columnprops#type), [`width`](https://react-api.syncfusion.com/data-grid/columnprops#width), and more. The `headerText` property specifies the title of the column header cell. When header text is not provided, the grid defaults to displaying the corresponding field name as the column title.

<Preview  frame={false}
 className={'sb-padding-10'}
  className={'sb-padding-10'}
  scroll={false}
  onDemand={true}
  content={<ColumnField />}
  path="grid/column-field.tsx"
/>

### Auto generated columns

When the columns are not defined in the `Columns` property, the grid automatically generates columns by extracting field names from the data source. Each field becomes a column, with the header text set to the corresponding field name.

<Preview  frame={false}
 className={'sb-padding-10'}
  className={'sb-padding-10'}
  scroll={false}
  onDemand={true}
  content={<AutoGeneratedColumns />}
  path="grid/auto-generated-columns.tsx"
/>

### Custom auto generated columns

Auto-generated columns can be customized at runtime using the grid [`onDataLoad`](https://react-api.syncfusion.com/data-grid/overview#ondataload) event to modify properties such as `format`, `textAlign`, `width`, and `headerText`. In the example below, specific fields are customized as follows:

- Book ID: Right-aligned, width: 95px.
- Title: Width: 170px. 
- Author: Width: 150px. 
- Genre: Width: 125px.
- Published Year: Right-aligned, width: 140px. 
- Rating: Right-aligned, width: 85px.

<Preview  frame={false}
 className={'sb-padding-10'}
  className={'sb-padding-10'}
  onDemand={true}
  content={<CustomizedAutoGeneratedColumns />}
  path="grid/customized-autogenerated-columns.tsx"
/>

## Complex binding

The Data Grid component supports nested data structures from local arrays and remote APIs. To bind and display hierarchical properties, use dot (.) notation within the [`field`](https://react-api.syncfusion.com/data-grid/columnprops#field) property.

### Local data
 
For local datasets with nested objects, use dot (.) notation in the `field` property to access inner-level properties.  

<Snippet content={gridComplexBindingSnippet}/>

<Preview  frame={false}
 className={'sb-padding-10'}
  className={'sb-padding-10'}
  scroll={false}
  onDemand={true}
  content={<ComplexColumns />}
  path="grid/complex-columns.tsx"
/>

### Remote data
 
To bind nested data from remote APIs (e.g., OData), use `DataManager` with a `Query` method to fetch expanded entities. Columns can access nested fields using dot (.) notation (e.g., Employee.City) for direct binding.

<Snippet content={gridRemoteDataMangerSnippet}/>

<Preview  frame={false}
 className={'sb-padding-10'}
  className={'sb-padding-10'}
  scroll={false}
  onDemand={true}
  content={<ComplexRemoteColumns />}
  path="grid/complex-remote-columns.tsx"
/>

> To bind columns from related entities such as Employee and Customer (e.g., Employee.Name, Customer.CustomerID) when using `DataManager`, apply the `expand` method to include those entities in the query: new Query().expand('Employee').expand('Customer').

### Rendering deeply nested columns

The Data Grid component supports traversal through deeply nested data structures using extended dot (.) notation in the `field` property. This allows direct access to multi-level properties, such as customer profiles or geo-location mappings.  

<Snippet content={gridComplexNextedColumnSnippet}/>

<Preview  frame={false}
 className={'sb-padding-10'}
  className={'sb-padding-10'}
  scroll={false}
  onDemand={true}
  content={<ComplexNestedColumns />}
  path="grid/complex-deeply-nested-columns.tsx"
/>

## Width

The [`width`](https://react-api.syncfusion.com/data-grid/columnprops#width) property sets column width using auto, pixel (px), or percentage (%) values. It helps optimize layout and readability by adjusting columns based on container size.  

### Auto width

Automatically adjusts each column’s width based on its content. When content exceeds available space, it is truncated with an ellipsis (...).

- Single column: A single column that dynamically adjusts its width to accommodate its content.

- Multiple columns: When widths are unspecified, space is allocated equally. Each column adjusts to its content's width, and the grid expands to accommodate all columns without scrollbars.

In this example, the "Ship Address" column width set as "auto".

<Preview  frame={false}
 className={'sb-padding-10'}
  className={'sb-padding-10'}
  content={<ColumnWidth />}
  path="grid/column-width.tsx"
  scroll={false}
  onDemand={true}
/>

### Pixel width

Sets a column’s width to a fixed pixel value, ensuring consistent sizing regardless of content or layout changes.

- Single column: Maintains a fixed width, which may lead to content truncation or unused space.

- Multiple columns: Each column retains its defined size, creating a structured and predictable layout.

<Preview  frame={false}
 className={'sb-padding-10'}
  className={'sb-padding-10'}
  content={<ColumnWidthPixel />}
  path="grid/column-width-pixel.tsx"
  scroll={false}
  onDemand={true}
/>

### Percentage width

Defines column width as a percentage of the available grid space, allowing dynamic scaling across different screen sizes or container dimensions.

- Single column: Adjusts with layout changes, maintaining its proportional width.

- Multiple columns: Percentage values are distributed across columns. In mixed setups (with pixel or auto widths), remaining space is allocated proportionally to percentage based columns.

In this example, the "Item" column width set as "25%".
<Preview  frame={false}
 className={'sb-padding-10'}
  content={<ColumnWidthPercentage />}
  path="grid/column-width-percentage.tsx"
  scroll={false}
  onDemand={true}
/>

### Adjusting column widths externally

This example shows how to dynamically adjust column widths using auto, pixel, or percentage values, with each column styled for clarity.

<Preview  frame={false}
  className={'sb-padding-10'}
  content={<DynamicColumnWidth />}
  path="grid/dynamic-column-width.tsx"
  scroll={false}
  onDemand={true}
/>

> * Columns without a set width automatically divide the available grid space equally.
> * A horizontal scrollbar appears when the total column width exceeds the grid container.

## Text alignment

The [`textAlign`](https://react-api.syncfusion.com/data-grid/columnprops#textalign) property adjusts text positioning within column cells, enhancing visual clarity and consistency. By default, text aligns to the left, but alternative alignments can be applied to meet specific layout requirements.

| Value     | Description                                     |
|-----------|--------------------------------------------------|
| `Left`    | Default alignment, aligns text to the left.  |
| `Center`  | Centers text horizontally for balanced display.  |
| `Right`   | Aligns text to the right, suitable for numeric data. |
| `Justify` | Distributes text evenly across the cell width.   |

<Preview  frame={false}
  className={'sb-padding-10'}
  content={<ColumnTextAlign />}
  path="grid/column-textalign.tsx"
   scroll={false}
  onDemand={true}
/>

## Visibility

By default, all columns in the grid are visible. To hide a specific column, set its [`visible`](https://react-api.syncfusion.com/data-grid/columnprops#visible) property to `false` in the [`Column`](https://react-api.syncfusion.com/data-grid/overview#columns) configuration. For example, the "Protein" column can be hidden by setting `visible` property to `false`.

<Preview  frame={false}
  className={'sb-padding-10'}
  content={<ColumnVisible />}
  path="grid/column-visible.tsx"
  scroll={false}
  onDemand={true}
/>
