import LiveSample from './live-sample.tsx';
import { Preview } from '../../common/preview';
import { Meta } from '../../common/meta';

<Meta
  title="React Data Grid - Real-Time Data Streaming | Syncfusion"
  description="Data Grid powers real-time trading dashboards, stock tickers, and dynamic UI updates with top performance."
/>

# Streaming Trade View with React Data Grid
 
This example showcases the React Data Grid component's capability to handle real-time data updates with high performance. The grid continuously refreshes to display live data changes, ensuring a current view. Designed for scalability, it efficiently manages large datasets while maintaining smooth and responsive interactions.
<Preview  frame={false} className={'sb-padding-10'} onDemand={true} content={<LiveSample />} path="grid/live-sample.tsx" scroll={false} />