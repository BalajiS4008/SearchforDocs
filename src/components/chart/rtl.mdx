import RTL from './rtl';
import { Preview } from '../../common/preview';
import { Meta } from '../../common/meta';

<Meta 
  title="React Chart Right-to-Left | Syncfusion"
  description="React Chart RTL mirrors layout for right-to-left languages, reversing axes, labels, legends, and tooltips."
/>

# RTL in React Chart

The RTL layout renders chart elements such as axes, labels, legends, and tooltips from the right side of the interface, flowing naturally toward the left. This layout is commonly used for languages like Arabic or Hebrew, where text and content are read from right to left.

<Preview onDemand={true} reset={true} content={<RTL />} path="chart/rtl.tsx" scroll={false} />
