import { TextArea } from "@syncfusion/react-inputs";

export default function App() {
    return (
        <div className='component-section'>
            <TextArea rows={3} placeholder="TextArea 3 rows" width={300} />
        </div>
    );
}